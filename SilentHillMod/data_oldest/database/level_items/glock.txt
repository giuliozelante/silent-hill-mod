#include <..\level_items.h>
#include <..\object_lods.h>
#include <..\weaponid.h>

[LOD]
{
    [Geometry]
	ExportData = ..\..\weapons\glock\glock_LevelItem.kf2;
    Distance = PROJECTILE_LOD_DRAW_ALWAYS;
}

[Properties]
TriggerRadius = RADIUS_BERETTA;
CantPickupMessagesTimeout = CANTPICKUP_TIMEOUT;
MatchingWeaponID = WEAPONID_GLOCK;
PickupIfNewWeapon = true;
PickupIfAmmoNotFull = true;

[OnFirstPickup]
{
	[Message] string = "activator->C_PickupWeapon( "glock" );";
	[Message] string = "activator->C_PickupAmmo( "beretta"," PICKUPAMMO_BERETTA ");";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_handgun );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_GLOCK1\" );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_GLOCK2\" );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_GLOCK3\" );";
}

[OnPickUp]
{
	[Message] string = "activator->C_PickupWeapon( "glock" );";
	[Message] string = "activator->C_PickupAmmo( "beretta"," PICKUPAMMO_BERETTA ");";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_handgun );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_GLOCK_END\" );";
}

[OnCantPickup]
{
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_ammo_full );";
	[Message] string = "maxpayne_hudmode->mphm_print( HUDPRINT_PICKUP_BERETTA_AMMO_FULL );";
}