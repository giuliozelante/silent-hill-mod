#include <..\level_items.h>
#include <..\object_lods.h>
#include <..\weaponid.h>

[LOD]
{
    [Geometry]
	ExportData = ..\..\weapons\bar\BrowningBAR_LevelItem.kf2;
    Distance = PROJECTILE_LOD_DRAW_ALWAYS;
}

[Properties]
TriggerRadius = RADIUS_SNIPER;
CantPickupMessagesTimeout = CANTPICKUP_TIMEOUT;
MatchingWeaponID = WEAPONID_BAR;
PickupIfNewWeapon = true;
PickupIfAmmoNotFull = true;

[OnFirstPickup]
{
	[Message] string = "activator->C_PickupWeapon( "bar" );";
	[Message] string = "activator->C_PickupAmmo( "bar"," PICKUPAMMO_BAR ");";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_rifle );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_BAR1\" );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_BAR2\" );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_BAR3\" );";
}

[OnPickUp]
{
	[Message] string = "activator->C_PickupWeapon( "bar" );";
	[Message] string = "activator->C_PickupAmmo( "bar"," PICKUPAMMO_BAR ");";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_rifle );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_BAR_END\" );";
}

[OnCantPickup]
{
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_ammo_full );";
	[Message] string = "maxpayne_hudmode->mphm_print( HUDPRINT_PICKUP_BAR_AMMO_FULL );";
}