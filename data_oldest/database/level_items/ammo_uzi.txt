#include <..\level_items.h>
#include <..\object_lods.h>
#include <..\weaponid.h>

[LOD]
{
    [Geometry]
	ExportData = Ammobox_Uzi.kf2;
    Distance = PROJECTILE_LOD_DRAW_ALWAYS;
}

[Properties]
TriggerRadius = AMMOBOX_RADIUS ;
CantPickupMessagesTimeout = CANTPICKUP_TIMEOUT;
MatchingWeaponID = WEAPONID_UZI;
PickupIfAmmoNotFull = true;

[OnFirstPickup]
{
	[Message] string = "activator->c_pickupammo( uzi, " AMMOBOX_UZI " );";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_ammo );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_UZI_AMMO\" );";
}

[OnPickUp]
{
	[Message] string = "activator->c_pickupammo( uzi, " AMMOBOX_UZI " );";
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_ammo );";
	[Message] string = "maxpayne_hudmode->mphm_print( \"HUDPRINT_PICKUP_UZI_AMMO\" );";
}

[OnCantPickup]
{
	[Message] string = "maxpayne_gamemode->a_playsound( level_items, pickup_ammo_full );";
	[Message] string = "maxpayne_hudmode->mphm_print( HUDPRINT_PICKUP_UZI_AMMO_FULL );";
}